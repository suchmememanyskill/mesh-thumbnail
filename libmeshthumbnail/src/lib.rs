pub mod mesh;
pub mod error;
pub mod parse_model;
pub mod extract_image;
pub mod scene;
pub mod render;

pub fn add(left: u64, right: u64) -> u64 {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
